*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
.gstack/
.claude/

# Benchmark raw cells + generated site (regenerated by ./benchmark.sh + bench/aggregate.py).
# Commit your own published run to gh-pages; don't ship partial/local data.
/results/*.json
/results/mem/
/docs/summary.json
/docs/*.html
/RESULTS.md
/MEM_RESULTS.md

# RoadRunner config — Octane `touch`es an empty one at runtime (root-owned via
# the container bind-mount). Runtime artifact, not source. See StartRoadRunnerCommand.
.rr.yaml

# Python bytecode (bench/aggregate.py is pure stdlib; no build artifacts tracked).
__pycache__/
*.pyc
